{
  "name": "elixir-mcp-collector",
  "version": "1.0.0",
  "description": "The operator-run collector for Elixir MCP: leases fetch jobs, calls the Clash Royale API with an IP-bound key, posts gzipped results. Never chooses its own targets; holds no user data.",
  "type": "module",
  "main": "src/index.mjs",
  "scripts": {
    "start": "node src/index.mjs",
    "test": "node --test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "verify": "npm run format:check && npm run test"
  },
  "dependencies": {
    "@aws-sdk/client-cloudwatch": "^3.600.0",
    "@aws-sdk/client-sqs": "^3.600.0"
  },
  "devDependencies": {
    "prettier": "^3.3.0"
  }
}
